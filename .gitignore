node_modules
.next
.turbo
# Private folder: never commit (secrets, internal docs, credentials)
/private

# Gevoelige gegevens: nooit committen (voorkomt diefstal van keys/emails)
.env
.env.*
!.env.example
.env wissel
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production
*.env
!.env.example
coverage
dist
*.log
# Stripe CLI binary (lokaal gereedschap)
/stripe
pnpm-lock.yaml
yarn.lock
# Vercel
.vercel
# Export for external review (optional, can be large)
PROJECT_EXPORT_FOR_REVIEW.txt

