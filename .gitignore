node_modules
.next
.turbo
# Private folder: never commit (secrets, internal docs, credentials)
/private

# Gevoelige gegevens: nooit committen (voorkomt diefstal van keys/emails)
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local
coverage
dist
*.log
pnpm-lock.yaml
yarn.lock

